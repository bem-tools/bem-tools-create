TODO:
1. logs
2. ? move templates to lib/templates

Use `braceExpansion` in JS API

Get rid of os-homedir -> tilde in honour of bem-config

вычислять текущую сущность по контексту


должно работать без .bemrc

починить
     bem-tools-create
        default scheme and default naming
    here b/b.css
    exists false
          1) should create a block using `nested` scheme and default naming
    before done
          2) should create a block using `nested` scheme and default naming


      0 passing (293ms)


options
    // {
    //     levels,
    //     scheme,
    //     naming,
    //     techs,
    //     root
    // }
